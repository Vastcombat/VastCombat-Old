/* document.getElementById("playButton").addEventListener(function(){ 
  OneSignal.push(function() {
    OneSignal.sendTag("hasPlayed", "true").then(function(tagsSent) {
      console.log("OneSignal | Tag \"hasPlayed\" has been set to \"true\"!");
    }); 
  });
} */

// Doesn't work anymore :(